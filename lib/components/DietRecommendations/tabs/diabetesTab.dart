import 'package:flutter/material.dart';

class DiabetesTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        color: Colors.grey,
        child: const Center(
          child: Text("Food to eat and food to avoid "),
        ));
  }
}
